% Fundamental concepts

tff(decl_real, type,
  real: $tType).

% Agents
tff(decl_agent, type,
  agent: $tType).

% Interface Descriptions
tff(decl_interface, type,
  interface : $tType).

% Phenomena
tff(decl_phenomenon, type,
  phenomenon: $tType).

% Formalism
tff(decl_formalism, type,
  formalism: $tType).

% Topics
tff(decl_topic, type,
  topic: $tType).
