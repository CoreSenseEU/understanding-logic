% Fundamental concepts

% Agents
tff(decl_agent, type,
  agent: $tType).

% Phenomena
tff(decl_phenomenon, type,
  phenomenon: $tType).

% Formalism
tff(decl_formalism, type,
  formalism: $tType).

% Topics
tff(decl_topic, type,
  topic: $tType).
