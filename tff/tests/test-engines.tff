include('tff/model/engines-and-modelets.tff').

tff(axiom_mass_requirement, axiom,
  ? [MS: modelet_set, E: engine, TS: template_set]:
  (
    is_interface_of(TS, E)
    & 
    interfaces_match(MS, TS)
  )
).

tff(test, conjecture,
? [E: engine, MS: modelet_set]:
  exertable(MS, E)
).
